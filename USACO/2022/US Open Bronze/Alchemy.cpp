#include <iostream>
using namespace std;

int main()
{
    // int N;
    // int n[N];
    // int K;

    int n[] = {2, 0, 0, 1, 0};
    int k[] = {(2, 3, 4),
               (1, 1),
               (1, 2)};

    return 0;
}